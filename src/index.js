import React from 'react';
import ReactDOM from 'react-dom/client';
import FlutterWrapperApp from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FlutterWrapperApp />
  </React.StrictMode>
);
